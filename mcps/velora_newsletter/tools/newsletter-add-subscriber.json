{
  "name": "newsletter_add_subscriber",
  "description": "Add an email address to the Verlin Labs newsletter subscriber list.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "email": {
        "type": "string",
        "format": "email",
        "description": "Subscriber email address"
      },
      "source": {
        "type": "string",
        "description": "Optional source label, e.g. MCP or partner list"
      }
    },
    "required": ["email"],
    "additionalProperties": false
  }
}