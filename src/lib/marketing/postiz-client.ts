/**
 * Deprecated and unused: the Marketing Board was rebuilt as a direct
 * integration (see meta-client.ts, linkedin-client.ts, accounts-store.ts,
 * posts-store.ts) instead of routing through a Postiz instance. Kept as an
 * empty placeholder rather than deleted, since this environment does not
 * allow file deletion - safe to remove this file in a future cleanup pass.
 */
export {};
